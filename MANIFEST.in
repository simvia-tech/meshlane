include LICENSE.txt
include CHANGELOG.md
# Ship the test suite, but not the (heavy) test meshes — those stay in the
# git/LFS repo. Keeps the sdist small.
recursive-include tests *.py
prune tests/meshes
